language: php

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - hhvm

sudo: false

install:
  - composer install --no-interaction --prefer-source

script:
  - vendor/bin/phpunit
  - vendor/bin/phpunit -c phpunit.integ.xml
